# Django
SECRET_KEY=change-me-to-a-random-secret-key using "python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'"
DEBUG=True

# LinkedIn OAuth (via django-allauth OpenID Connect)
LINKEDIN_CLIENT_ID=your-client-id
LINKEDIN_CLIENT_SECRET=your-client-secret

# Inngest (dev mode uses docker; prod needs these)
# Set to 1 for local Docker-based Inngest
INNGEST_DEV=1
INNGEST_EVENT_KEY=test
INNGEST_SIGNING_KEY=test
